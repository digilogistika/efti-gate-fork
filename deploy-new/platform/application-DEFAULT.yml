server:
  port: 8070

gate:
  minSleep: 1000
  maxSleep: 2000
  owner: ${GATE}-platform
  cda_path: file:/usr/src/myapp/cda/
  gate: ${GATE}
  gateBaseUrl: http://${GATE}-efti-gate:8880
  platformBaseUrl: http://${GATE}-efti-platform:${server.port}
  gateSuperApiKey: ${ADMIN_API_KEY}

