spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://auth.gate.eu:8080/realms/eFTI

server:
  port: 8070

gate:
  minSleep: 1000
  maxSleep: 2000
  owner: ${GATE}-platform
  cda_path: file:/usr/src/myapp/cda/
  gate: ${GATE}
  ap:
    url: https://${GATE}-harmony-platform-1:8443/services/wsplugin
    username: service_account
    password: Azerty59*1234567

